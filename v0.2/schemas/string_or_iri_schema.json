{
  "$schema": "http://json-schema.org/draft-04/schema",
  "title" : "MIACME string or IRI schema",
  "version" : "0.2",
  "description" : "JSON-schema representing a string or an IRI (for ontology term or database accession) in MIACME",
  "type" : "object",
  "properties" : {
    "anyOf": [
      { "type": "string" },
      { "type": "string", "format": "uri"}
    ]
  }
}